apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "praxis.serviceAccountName" . }}
  labels:
    {{- include "praxis.labels" . | nindent 4 }}
# praxis needs no Kubernetes API access: never mount a token (invariant 9).
automountServiceAccountToken: false
